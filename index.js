let configSce = require("@nxn/config");
let bootSce = require("./boot.service");
let FlowNode = require("./node");

/*
const express = require("express");
const app = express();
*/

module.exports = {configSce,bootSce,FlowNode}